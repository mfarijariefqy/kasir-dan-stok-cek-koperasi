-- Create database for Kasir UMKM
CREATE DATABASE IF NOT EXISTS kasir_umkm CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- Use the database
USE kasir_umkm;

-- Grant privileges (if needed)
-- GRANT ALL PRIVILEGES ON kasir_umkm.* TO 'root'@'localhost';
-- FLUSH PRIVILEGES;
